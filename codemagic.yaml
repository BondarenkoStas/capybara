# Automatically generated on 2020-04-29 UTC from https://staging.codemagic.io/app/5c80df6467038200016d45e8/settings
# Note that this configuration is not an exact match to UI settings. Review and adjust as necessary.

workflows:
  default-workflow:
    name: Default Workflow
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: 'try'
          include: true
          source: true
    scripts:
      - echo "export MY_VAR=$FCI_BUILD_DIR" >> migration_variables
      - |
        source migration_variables
        echo $MY_VAR
      - echo $(bash script.sh)
      - cd android && cat ffile
